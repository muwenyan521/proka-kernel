title = "Output"

[[config]]
name = "SERIAL_LOG_PORT"
type = "hex"
default = 0x3F8
desc = "Serial Port for Logging"
help = "The I/O port address for the serial port used for kernel logging (default 0x3F8 is COM1)."
